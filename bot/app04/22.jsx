//usestate lazystate

const App=()=>{







    return (
        <button onClick={()=>{
            setCount(count+1);
        }}></button>
    )
}

/**
 * return(
 * <input onChange={
 * (event)=>{
 *   setFirstName(event.target.value)}
 * value={firstName}}>)
 */

//value is written in html input direcltly


return



















